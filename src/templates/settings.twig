{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Build hook URL",
    instructions: "Enter the URL to call to start the build",
    id: "webhookUrl",
    name: "webhookUrl",
    value: settings.webhookUrl,
    required: true
}) }}