{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Webhook URL",
    instructions: "Enter the URL to fire when the button is clicked.",
    id: "webhookUrl",
    name: "webhookUrl",
    value: settings.webhookUrl,
    required: true
}) }}